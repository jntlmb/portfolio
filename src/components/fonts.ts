import {
  Merriweather,
  Edu_QLD_Beginner,
  Zilla_Slab,
  IBM_Plex_Mono,
} from 'next/font/google';

export const merriweather = Merriweather({
  weight: ['400'],
  subsets: ['latin'],
});

export const eduQLD = Edu_QLD_Beginner({
  weight: ['400'],
  subsets: ['latin'],
});

export const zillaSlab = Zilla_Slab({
  weight: ['400'],
  subsets: ['latin'],
});

export const ibmPlexMono = IBM_Plex_Mono({
  weight: ['300'],
  subsets: ['latin'],
});
